function solution() {
  function giftBoxes(g, c) {
    /*
     * Return the number of gift boxes that can be packaged.
     */
  }

  return {
    giftBoxes: giftBoxes
  };
}

module.exports = solution;
